# TyvaTrail

Чтобы развернуть сайт локально на Windows, введите следующие комманды:

```shell
python -m venv .venv
.\.venv\Scripts\activate.bat
pip install -r requirements.txt
python manage.py runserver
```
